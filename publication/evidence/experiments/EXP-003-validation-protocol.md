# EXP-003 R03 validation contract

2026-09-12. Bounded implementation validation, preceding fresh development and confirmation. The unchanged EXP-002 implementation and measured results remain hash checked. No parameter selection or H3 significance test occurs here.

The main four growth rules and all five sizes follow [EXP-003](EXP-003.md). Graph random streams use PCG64 SeedSequence `[3,0,block,replicate,size,component]`: component 1 selects shared donors, 2 samples structured wiring, 3 samples uniform wiring, 4 shuffles the structured added edges, and 5 assigns tie priorities. Validation blocks are 0–4 and replicates 0–2. Donor and added-node ordinals are zero based; added nodes are ordered by stratum and then donor draw. Largest remainders use exact integer arithmetic. Native matrix indices retain the audited dataset numbering. No new external source is introduced.

Validation deliberately reuses EXP-002 **stage-zero** exogenous task streams and probe draws through a restricted adapter, so N=73 can be compared exactly. Inputs, action-selection uniforms, both potential outcomes and cue ordering are paired across arms, sizes and graph replicates. Realized choices may differ as learners diverge. N=73 uses the baseline tie priority unchanged. At larger sizes, a uniform permutation of all priorities interleaves added cells with native cells while preserving the native relative order. Priorities are paired across wiring arms. The bridge rejects development/confirmation tasks. Their fresh experiment-3 streams, recovery contract and shared nine-setting selection must be frozen before that campaign.

Acceptance: verify 300 graphs (5 blocks × 3 replicates × 5 sizes × 4 arms), all donor/count/degree invariants, unchanged native columns, nonzero normalized inputs, exact graph reconstruction and deterministic replay. Degree nulls must pass the specified mixing threshold; successful swaps and overlap are retained for the diagnostic graphs. Test a deliberately unmixable fixture. Compare no-growth full traces, probes and final weights exactly for all four arms, both task families and proportional/fixed-four sparsity. Check grown scalar/batched learning, frozen probes, and resumption at trials 0,1,127,128,255,256,383 for both families and full/fixed-four/bottleneck controls. A checkpoint must reject a different graph. Independently calculate the 32-bin bottleneck.

Descriptive diagnostics use validation block 0, all 20 episodes, three graph replicates, all sizes and four arms. Reuse N=73 once. Add fixed-four sparsity diagnostics at N=110 for clone/structured/null. Count activity across both presented cues (15,360 presentations per graph), before learning and before any readout bottleneck. Report individual participation, native/added unused fractions, covariance participation rank, redundant normalized input columns, added distinct nonzero activity patterns, and added centered linear dimensions conditional on the surviving native activity. Linear rank uses a shared eigenvalue tolerance `max(largest full covariance eigenvalue,1)*1e-10`; it is distinct from participation rank. New activity dimensions in clones can arise from winner competition and tie ordering despite identical input columns. These measurements are input/encoder-specific, not biological inactivity or evidence of learned utility.

For each diagnostic configuration, run two complete learning smoke episodes (0 and 10), using the EXP-002 development-selected generic delta at eta=.01 and T=.1. Also run the 32-feature bottleneck at N=73 once and N=110 structured/null across three replicates. Retain exact graph lineage, packed cell activity, traces, probes, final weights, environment, code/data hashes and measured resources. Do not interpret these two-episode scores as capability estimates. Exact activity recovery from saved bits is checked. The full-native uniform-replacement exploratory arm is outside R03's main-growth scope and remains to implement before a campaign that includes it.

Reproduce with `python tools/validate_growth.py --output results/validation/exp003-replay --report research/exp003_validation_replay.json` using Python 3.12 and NumPy 2.3.5. Existing destinations are rejected to preserve prior records. The runner executes all baseline and growth tests, checks the baseline gate, and writes a new record only after success. [Validation results](EXP-003-validation-results.md) distinguish operator readiness from an untested growth benefit.
