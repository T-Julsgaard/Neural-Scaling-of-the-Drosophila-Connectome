# EXP-009 / Session A: bounded published-parameter positive control

2026-09-15. Prospective protocol v1, written before generating validation or new development/evaluation tasks. Session A only; stop at its decision gate.

## Question and selected comparison

Does the supplied Abdelrahman, Vasilaki and Lin (2021) threshold-compensated model improve held-out odor-valence choice probability over its paired random uncompensated model? Reference: Figure 4B2, magenta versus red, c=1, 100 synthetic odors, 24 PNs, 2,000 KCs. Expected direction: compensated > uncompensated. Audit published Dataset S1, Fig 4!I3:I22 and M3:M22 separately; those historical values are not new evaluation evidence.

Pin author repository `aclinlab/CompensatoryVariability` at `9f3f7e9e85117febef1ad32e3152c830570f74d3`, including GPL-3.0 license, supplied `Data_submitted_fly_wNoise11.mat`, methods, evaluation function, noise function and `hallem_olsen.mat`. Use the supplied instance's `thisW`, `thetaS`, `APLgains(1)` versus `thisW_Kennedy`, `theta_Activity_homeo`, `APLgains(5)`. Verify correspondence of projections and expose any extra scaling. Do not invent regenerated fitted models or select a favorable instance.

**Classification:** independent bounded transfer/adaptation using author-fitted parameters, with equation-level author-runtime validation. Not exact Figure 4 reproduction. Only one fitted instance is supplied; the 20-instance calibrated ensemble, initialization RNG and complete figure-generation/learning-rate selection provenance are not supplied in the inspected tree. Reconstructing all calibration would require a separate bounded implementation of the joint optimizer. Here no optimizer is rerun and no claim of its validation is made. Available source/data permit this more limited test; they are not claimed globally unavailable.

## Frozen implementation and access

Encode SI Eq.3: `relu(W.T @ x - alpha * sum(W.T @ x) - theta)`. Graded responses, no top-K. Saved thresholds include fitted global scale. Audit original calibration responses against target .51 +/-6%, coding level .10 +/- .01 and no-inhibition / inhibition ratio 2 +/- .2; a mismatch remains disclosed and does not get repaired using new tasks.

Create 100 fresh synthetic prototypes per block by independently sampling each PN from the first 110 rows of supplied Hallem-Olsen PN responses (SI input sampling; no ORN transform applied twice). Recover the fixed PN affine scale from author fixture clean first trials and MATLAB histogram centers exactly as in author source lines 38-90. Validate recovery. Use that scale for every block; clip noisy raw PN rates at zero before scaling and floor scaled values at zero. Do not estimate any scaler from test trials. Noise uses the author nearest-bin Bhandawat SD table, one independent Gaussian draw per PN/odor/trial; 15 independent noisy training and 15 independent noisy probe trials. Unlike original code, no special clean first training trial. Prototypes, labels and trial noises are shared between arms. Balanced 50/50 random valences and common independent Uniform[0,1) output-weight initialization per block.

Each arm rescales graded responses by its **training-only** global maximum; test values may exceed one and are not clipped. Source code rescales using all trials, which this adaptation intentionally avoids. Learning follows SI Eq.20 / author lines 1556-1687: depress only the wrong-valence output weights by `exp(-eta / mean(Ytrain) * sum_trials(Ytrain))`, for every training odor, with full labels and no action-dependent sampling. This is commutative multiplicative depression, not our sequential prediction-error learner. Test SI Eq.21 / unmodified author function: mean correct-valence softmax probability at c=1, not sampled classification accuracy.

No new input calibration. Author parameters were fitted on their old odor pool and are frozen; new prototype draws are independent from that pool but drawn from the same empirical marginals. The empirical PN table and fixed author model are conditioning information. Within each new task, labels are visible only during readout learning. No evaluation data enter fitting, normalization, tuning or sample sizing.

## Validation, development, precision, decision

Dedicated root seed 9009, stream stages validation=0, development=1, confirmation=2, with separate substreams for prototypes, noise, labels and weights. Tests never construct stage 1/2 inputs. Validate scalar versus vectorized excitation/inhibition/ReLU, multiplicative learning, softmax, label complement, zero-learning chance with symmetric initialization, memory nonmutation, held-out scaling, stream separation and deterministic replay. Run a supplied-input Octave fixture using source-preserved encoding/learning arithmetic and the unmodified author evaluator; require max absolute discrepancy <=1e-10 for features, learned weights and probabilities. This validates arithmetic, not the homeostatic optimizer or biological claims.

Four fixed development blocks 0-3. Equal ten-rate grid per arm: 10**[-5,-4,-3,-2.75,-2.5,-2.25,-2,-1,0,1], c=1 throughout. One global rate per arm maximizing mean development probability; ties within 1e-12 select the earlier/smaller rate. No grid extensions. Also freeze a descriptive compensated-at-uncompensated-rate contrast to expose tuning dependence.

After development freeze rates and n before constructing confirmation. n=ceil((1.96*1.25*s/.02)^2), using paired block SD at selected rates; round up to multiple 8 and constrain to 24-48. Record uncapped n and projected precision. Fixed fresh confirmation blocks 0..n-1; no exclusions or sample extension. Independent task blocks, conditional on ONE fixed author network, are uncertainty units. Paired percentile bootstrap, 50,000 draws, seed 900991, two-sided 95% interval; one primary contrast. Approximate intervals, no animal/network-population inference.

Positive-control gate requires all arithmetic/data/replay audits pass, compensated mean probability >=.55, primary lower CI >.03, and interval halfwidth <=.02. Three points is a prospective practical convention, not a biological threshold. If positive but too imprecise, or margin not established, gate is inconclusive/failed as appropriate. Preserve all outcomes. No automatic alternative implementation, panel or tuning search. B can proceed without a validated positive effect but cannot explain/overturn the published effect. Passing supports a bridge only to this supplied-parameter regime.

## Resources and recovery

Use existing local Python/NumPy and Octave, one worker, one BLAS thread. Validate one full-size workload and set measured numerical campaign cap before development. Absolute ceilings: 30 minutes combined development/confirmation compute, 2 GiB peak process memory, 1 GiB new run outputs, max 48 confirmation blocks. Persist atomic per-block NPZ (inputs, labels, initial/final weights, scalers and probe probabilities), JSON counters and SHA256. Resume only verified blocks under identical protocol/source/data hashes. Stop on nonfinite states, checksum/source drift, failed tests or caps; preserve partial/failure evidence. Do not modify EXP-002-008 frozen scientific sources/results. Hash them before new work and verify at completion.

Close with source/equation mapping, actual resources, uncertainty, failure history, compact comparison to our encoder/learner, at most two ranked candidate factors, and exact Session B handoff. No Session B experiment, growth, new anatomy, paid compute or publication.
