# EXP-010 / Session B: information versus sequential readout learning

Prospective development protocol, 2026-09-15. No EXP-010 outcomes inspected.

## Scope

Use the frozen EXP-007 D05 normalized projection, priority and independent unlabeled calibration (strength .25), K=6, native and all three degree/contact-preserving random projections (ordinary and their own .25 calibration), plus direct PN. No new compensation fit, anatomy or Session C. Session A is a validated fixed reference, not a causal cross-task comparator. Investigate only its nominated update-dynamics factor; no normalization factorial or author-model manipulation is justified by the preliminary diagnostic alone.

Fresh EXP-007 generator blocks 101000..101005 development and 102000..102023 confirmation, separate stage namespaces; overlap 6, load 16, 128 presentations/pair, no reversal, training noise .1; 64 held-out probes/pair at .1 and .3 plus clean prototypes. These high block IDs are new and are checked against historical records. Independent task blocks are inference units, conditional on fixed anatomy/calibration. Random instances are averaged within blocks.

## Access and controls

Existing opponent chosen-action clipped delta, eta .006666666666666667, temperature .1, initial plus=minus=.1. At each presentation it sees two observations but only its chosen stochastic +/-1 outcome. Archive its choices. Offline ridge `w` predicts the SAME scalar stochastic outcomes: (a) only archived chosen observations/outcomes; (b) both observations/outcomes. Neither receives latent .8/.2 probabilities or clean labels. Supervised online clipped delta sees both outcomes on exactly those observations, cue 0 then cue 1. An unclipped opponent delta arm changes clipping only. A replay control performs 10 blocked passes over the same full observations/outcomes; it has no extra data but extra computation. This separates access, clipping and revisiting labels, although the residual offline contrast still combines objective solution and schedule. Full supervision doubles observed outcomes and updates and is explicitly privileged versus chosen feedback.

Offline signed linear ridge has no intercept (matching opponent difference), minimizes mean squared outcome error plus lambda*||w||^2. Solve normal equations, verify relative residual <1e-9; grid lambda={1e-6,1e-4,1e-2,1e-1}, pick per representation/access on development held-out .1 probes only. No test fitting. Signed weights can be represented EXACTLY as two nonnegative opponent weights; coordinate clipping is a learning trajectory constraint, not a restriction of representable signed readouts. Archive this decomposition check.

Native sparse features have squared norm 100/6. Report direct both raw and multiplied by a training-only scalar to match mean squared norm 100/6. All core comparisons use matched norms, same eta and temperature. Report raw direct as a scale sensitivity. Sparse ordinary/calibrated/random norms are exactly identical. No rate tuning; conclusions are about this fixed existing learning regime.

## Measurements and selection

Primary: deterministic fraction of held-out .1 old-pair preferences correct (pairs 0..14; tie=.5). Also soft probability at temperature .1, all-pair and worst-pair accuracy, .3 noise, clean accuracy, acquisition after each pair, and signed margin change for EVERY old pair after EVERY subsequent update (including replay). Save clean/noisy margins and full update traces. Clean prototype rank, training rank and nonzero singular condition number, noisy within-cue deviation, clean pair distance and feature norms describe geometry; they cannot establish an information-theoretic bound.

After six development blocks, select one explanation/prediction only if optimization, stream, access, update and replay tests pass and the native/calibrated mean offline-full minus supervised-online old accuracy >=.10 with offline >=.80. Candidate prediction: on fresh tasks, offline retains recoverable old labels while full-supervised blocked online loses acquired margins; full supervision alone does not close the gap. Freeze selected regularizers and 24-block comparison BEFORE generating confirmation. Confirmation success requires paired 95% Student-t lower CI of offline-full minus supervised-online >.05, offline-full mean >=.80, and lower CI of online acquisition-minus-final old accuracy >.05. Native/calibrated are averaged within blocks for this ONE primary contrast; other arms descriptive. Failure of any criterion falsifies this operational prediction; a narrower mixed/unresolved conclusion remains valid. No sample extension. If development gate fails, stop with narrower evidence for D and do not confirm a correlation.

## Validation, audit and resource cap

One local worker and BLAS thread, <=30 minutes campaign compute, <=2 GiB process memory, <=1 GiB outputs. Validate scalar existing update equivalence, analytic ridge versus augmented least squares, signed/nonnegative equivalence, orthogonal learnability, observation/access counts, stream separation, update-margin telescoping, scale equivalence, and author multiplicative order invariance. Time a complete validation block before development; require threefold projected total <=30 minutes. Freeze source/protocol/calibration hashes, save source copies and atomic per-block NPZ+JSON, resume only checksummed matching blocks. Audit all arrays by deterministic recomputation plus source/input hashes. Preserve EXP-002..009; hash the scientific dependencies and prior compact records before/after (not a claim of a full historical archive audit). Record failures and actual resources. No external literature assertions beyond the already audited Session A handoff.
